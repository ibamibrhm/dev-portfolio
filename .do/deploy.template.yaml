spec:
 name: dev-portfolio
 static_sites:
  - name: dev-portfolio
    git:
      branch: master
      repo_clone_url: https://github.com/ibamibrhm/dev-portfolio.git
    build_command: yarn build
    output_dir: build
    environment_slug: html